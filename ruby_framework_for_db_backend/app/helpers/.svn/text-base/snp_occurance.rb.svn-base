class SNPOccurance

  attr_reader :base
  attr_reader :counts
  attr_reader :forward_qual
  attr_reader :reverse_qual

  def initialize(base, counts, forward_qual, reverse_qual)
    @base, @counts, @forward_qual, @reverse_qual = base, counts, forward_qual, reverse_qual
  end #
end # class
