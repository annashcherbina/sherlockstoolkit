<%= render(partial: 'layouts/error_messages', locals: {object: @attribute}) %>

<%= form_for(@attribute) do |f| %>

    <div class="field">
        <%= f.label :name %><br>
        <%= f.text_field :name %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
